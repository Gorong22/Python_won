[build]
  # Netlify가 public 폴더를 배포 디렉토리로 사용
  publish = "public"
  
  # 빌드 명령어 (필요한 경우)
  # command = "npm run build"

# SPA 라우팅을 위한 리다이렉트 규칙
# 모든 경로를 index.html로 리다이렉트 (클라이언트 사이드 라우팅 지원)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
